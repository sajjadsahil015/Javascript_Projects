# UI Logic Documentation

This document will detail the UI interactions and DOM manipulation logic.

## Key Functions
- `addTransactionDOM(transaction)`: Adds a transaction item to the list.
- `updateValues(transactions)`: Updates the balance, income, and expense displays.
- `removeTransactionDOM(id)`: Removes a transaction from the UI.
